{
  "name": "agentic-mcp-server",
  "version": "1.0.0",
  "description": "Remote MCP server for multi-agent development workflows — Cloudflare Workers + Durable Objects",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.0.72",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250430.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.0.0"
  }
}
